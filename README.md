# Inventory Service

ShopEasy inventory API — standalone microservice repository.

Port: `5003` | API: `/api/inventory`

```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt && python app.py
```

See `repos/product-service/` for the full Jenkins promotion pipeline.
