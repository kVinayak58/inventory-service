name: CI

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

jobs:
  checks:
    uses: kVinayak58/platform-config/.github/workflows/python-microservice-ci.yml@main
    with:
      service_name: inventory-service
      ecr_repository: shopeasy-inventory-service
    secrets: inherit
